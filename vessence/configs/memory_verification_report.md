# Memory Verification Report — 2026-05-22 02:36

Checked: 20 | Stale: 19 | Fixed: 19 | Deleted: 0 | Errors: 0 | Skipped recent: 66

- **UPDATED** `e75d7ba1-c90` — Confirmed against configs/MCP_SPEC.md, jane/tool_loader.py, live skill and essence directories, jane_web/main.py, jane_web/jane_v2/models.py, jane_web/jane_v3, and legacy gemma_router/gemma_stage2 comments/imports. Codex was mostly right, but the corrected memory should mention daily_briefing.mcp.json is not loader-recognized and that current env enables v3 ahead of v2.
- **UPDATED** `0b0896df-9a4` — Codex was directionally right, but the actual code shows an additional non-MCP manifest essence path, so the correction needs to be broader than Codex's suggested text.
- **UPDATED** `69a2fdbe-225` — Confirmed against current `jane/tool_loader.py`, `context_builder/v1/context_builder.py`, `jane_web/jane_proxy.py`, and the installed `~/ambient/skills` tree; Codex's stale verdict is correct.
- **UPDATED** `a3c5e3ea-f08` — Codex was right: the old resolver and jane_proxy call sites still exist, but the memory missed the active v3-preferred chat path and the active jane_v2 music_play handler path; Gemma references are legacy names in current code.
- **UPDATED** `160c0bce-9ec` — Codex was directionally right that the architecture is only partial, but the audit found corrections: `/home/chieh/ambient/tools` is a symlink to `skills`, Gradle is not broken in the live filesystem, and crontab is readable and sets `TOOLS_DIR=/home/chieh/ambient/skills`.
- **UPDATED** `4d22a27f-a20` — Confirmed against current code and filesystem. Codex was right that the root/config claims mostly still hold, but the loader uses JANE_TOOLS_DIR directly, installed tool folders are mixed-format, and Android tool registration is still partly hardcoded.
- **UPDATED** `ac06e647-af7` — Codex was right that the memory was truncated and partially stale; the live code confirms the loader path/env behavior, tool hook protocol, context/proxy integration, Android source generator, symlink layout, loaded tools, and hook configuration, with wording tightened so static hygiene is described as a supporting repo check rather than overstated.
- **UPDATED** `0845b572-f6a` — Actual code confirms Codex's partial verdict: the hook and loader facts mostly match, but the loader now defaults to `~/ambient/skills`, Gradle/hook still use `~/ambient/tools`, and multi-tool commits only warn.
- **UPDATED** `0e79d2ac-b55` — Code confirms the original memory was partially stale: user/session keying is accurate for JaneSessionState and persistent managers, but standing_brain.py is singleton/global rather than per user/session. Codex's crontab side note is not true in the current environment, but the memory itself has no cron claim.
- **UPDATED** `df317381-9eb` — Confirmed from source and docs: the original queued architecture memory is historically real but stale as a pending Phase 7 blocker and wrong if read as current implementation. Codex was broadly right, except live crontab is now readable here and includes the marketplace cron.
- **UPDATED** `cad1cf8e-da6` — Confirmed in code: ApiClient.kt now uses 600s, jane_proxy.py emits 3s heartbeat payloads, main.py serves chat streams as application/x-ndjson, and Android retry code exists but no last-event watchdog/reconnect implementation was found.
- **UPDATED** `6d032384-520` — Confirmed by reading Android TTS call sites, HybridTtsManager USE_SERVER_TTS=false, the Daily Briefing TTS no-op stub, XTTS route/code presence, Kokoro references, news_fetcher model default, and live crontab. The old XTTS-for-summarization claim is stale.
- **UPDATED** `9adcf15a-cec` — Verified against TODO_PROJECTS.md, sms_helpers.py, send_message handler, vault_web SQLite aliases, clinic schedule code and schedule.db, Waterlily repo docs/backend, running processes, cron docs/logs, and py_compile showing the Kathia scraper IndentationError.
- **UPDATED** `a377227d-b22` — Confirmed in AndroidManifest.xml, MainActivity.kt, MessagesReadInboxHandler.kt, ClientToolDispatcher.kt, SmsSyncManager.kt, MessagesFetchUnreadHandler.kt, jane_web server code, and configs; the original memory's UI automation plan/status is stale while the READ_SMS/direct message-reading parts are current.
- **UPDATED** `8ff0667e-46c` — Stale. Code confirms the Gemma/router and Claude standing-brain claims are outdated. Codex was mostly right, but it missed that this runtime enables v3 before v2.
- **UPDATED** `81ce9c38-874` — Codex was right: the original memory is truncated, and the current code confirms the shared resolver, endpoint/proxy/v2 call sites, and duplicated v2 playlist matching.
- **UPDATED** `8c7cda38-d7f` — Codex was right: route/env/registry/handler claims are current, but the stored memory is truncated and stale about Gemma; active v3 classification is the qwen-backed v3 classifier.
- **UPDATED** `ce56ca65-8a9` — Confirmed from code: Codex was right that the old memory's v2 routing details were stale; v2 goes through stage1_classifier.py plus _gate_check, while v3 uses the v3 Chroma+local-LLM classifier and inline short-circuit.
- **UPDATED** `2dc3dec8-82f` — Codex was right that the core paths and architecture still exist, but the memory is truncated and needed current details from the actual pipeline, classifier, dispatcher, model, and Stage 3 code.
