# Memory Verification Report — 2026-05-24 03:04

Checked: 20 | Stale: 20 | Fixed: 20 | Deleted: 0 | Errors: 0 | Skipped recent: 99

- **UPDATED** `3360bc85-083` — Verified against the design note, v3/v2 classifier code, pipeline selection, runtime env, and FIFO modules; Codex was right that the note is historical and not the current implemented classifier.
- **UPDATED** `054561e1-e57` — Codex was partly right about the files, priority, configured workflow pieces, OpenAI model, missing cache, and cron removal, but missed that the extractor is currently broken by the Progress Tracker heading mismatch.
- **UPDATED** `e17cda7c-016` — Confirmed against pipeline.py, stage1_classifier.py, intent_classifier/v2/classifier.py, stage2_dispatcher.py, models.py, and clinic_schedules_info files; original memory was mostly right but truncated and missing the current params/default-loader behavior.
- **UPDATED** `ead0d5ad-0a7` — Code checks matched the package, SHA-1, client ID, auth path, and current version, but the runtime OAuth registry shows the Android client was registered and confirmed working, so the original pending-fix wording is stale.
- **UPDATED** `6e76b008-7f1` — Codex was right: hashes, docs, assets, and sample count match, but the code only loads openwakeword/$wakeWordModelName with default hey_jane.onnx; current call sites do not pass hey_jane_v7_backup.onnx.
- **UPDATED** `58f61412-713` — Confirmed in stage2_dispatcher.py, classes/__init__.py, pipeline.py, and representative handlers. The old memory was partly right about successful Stage 2 dicts and text, but overbroad about all handler returns requiring text and wrong about pending_action as a generally consumed top-level field.
- **UPDATED** `ef5612fc-3ee` — Confirmed against handler.py and models.py. Codex was right: the main context-provider architecture still matches, but the literal one-SQL-query claim is stale because the code first queries week_start and some loaders perform repeated day-row queries.
- **UPDATED** `d2d2e095-149` — Verified the actual source files, cache, logs, and live crontab. Codex was right: the old memory was stale about Stage 3 reading only the cache and too rigid about fixed category wording.
- **UPDATED** `5001554f-1cc` — Original memory is stale because it is truncated at the log path. Actual code, docs, data files, logs, and live crontab confirm the corrected details; Codex was only wrong about live `crontab -l` being unreadable in this environment.
- **UPDATED** `1ec8aa82-4b3` — Codex was right: the stack, GCP project/service, Cloud SQL instance/DB, and GCP-only runtime are confirmed by code/docs and live gcloud; the legacy path is absent, the exact 2026-04-30 02:21 UTC cutover timestamp was not found, and CUTOVER_RUNBOOK.md is no longer a future runbook.
- **UPDATED** `eef09673-adb` — Verified against package metadata, imports, source files, live crontab, profile directories, logs, and a direct headless launch. The old memory had a truncated invalid profile path; Codex was mostly right, but the sandbox launch-failure caveat did not reproduce in this environment.
- **UPDATED** `bbd47971-bdc` — Confirmed against the actual repo: Codex was right that the only stale claim is scripts/migrate_from_rds.sh being present; the other artifact, dev-login, deploy env, and Cloud SQL socket claims still match the code.
- **UPDATED** `5d6c603e-98e` — Confirmed in current models, curriculum service, teacher route/template, and migrations 0004/0009/0010; the old right-rail DRAFT/HOMEWORK/EXAM per-module model is stale.
- **UPDATED** `331fa4d4-25e` — Original memory is stale for Cloud Run revision and Alembic head. Codex correctly identified those fields as stale, but its suggested 00031/0036 correction is also stale; code and live checks show 00069 and 0048.
- **UPDATED** `52389d73-e52` — Code confirms the model/form/route drift, live DB confirms INT, but the CLAUDE.md documentation claim is false.
- **UPDATED** `acacc0e7-035` — Verified against the repo: HW2_TODO.md is absent, rref_B has q1.py-q10.py, the audit report lists rref_B as 10/0/0, and the runtime still combines static registry problems with custom problem rows.
- **UPDATED** `6a405726-398` — Confirmed against /home/chieh/code/chieh_class_v2: the old Alembic head 0032_prompt_override_source_hash is stale; `python -m alembic heads` reports 0048_prompt_source_signature, while the problem-storage sandbox and calendar claims still match the code.
- **UPDATED** `95db60cc-111` — Verified against unit files, repo architecture/backend routes, Cloudflare API, DNS, ss/process list, and curl. Original memory had stale 8089 target and truncated CF token path; core zone/tunnel identity was right.
- **UPDATED** `d17c2f11-9aa` — Codex's partial verdict is right: service files, backend routes, docs, live localhost checks, and absent local Cloudflare ingress config confirm the stable current facts but not the remote/historical tunnel claim.
- **UPDATED** `aaf029b2-47f` — Confirmed Codex is right: code search/read found no Vessence implementation calling api.ipify.org or managing Cloud SQL/AWS/firewall IP allowlists. Current Vessence code/config points to Cloudflare Tunnel/reverse_proxy plus local ChromaDB/SQLite; Cloud SQL only appears as a theme label, test fixture, and separate homework-audit skill using cloud-sql-proxy at 127.0.0.1:3307.
