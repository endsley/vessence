# Memory Verification Report — 2026-06-08 02:15

Checked: 20 | Stale: 15 | Fixed: 14 | Deleted: 0 | Errors: 0 | Skipped recent: 185

- **UPDATED** `99c0b500-ab2` — Verified against config, MCP bridge code, AGENTS.md, and the query_live_memory shim; the old memory was truncated and omitted the current broader-query fallback.
- **UPDATED** `79101a69-c26` — Source confirms the memory was truncated/partial and mostly matches current code; corrected wording adds the exact admin fallback order and fixes the nuance that `user_admin` is accepted from any existing per-user config.json, not strictly only managed users.
- **UPDATED** `e44d09fd-ef8` — Actual code and live crontab confirm the core facts; update fills in the truncated failure behavior and adds dated live-cron verification.
- **UPDATED** `02389095-cd2` — Verified main.py routing, v3 resolver order, classifier entrypoint, active env/process flags, and local model code. Codex was right that the old classif symbol was stale.
- **UPDATED** `78c280d2-bc1` — Confirmed in code and live schema: ledger path resolves from VAULT_HOME, turns has the listed columns including cls, and _log_to_ledger is called from both add_message() and add_messages(). The stored memory was truncated.
- **UPDATED** `27fda530-892` — Codex was right to mark the memory partial: the ledger path and core wiring are correct, but the memory is truncated/underspecified and the completed-turn-only wording is inaccurate. Codex's crontab limitation did not apply in this session; live crontab was readable.
- **UPDATED** `f8b1f197-8f7` — Confirmed from live code: ledger path and turns schema are still valid, add_message/add_messages call _log_to_ledger, and jane_proxy now defines/calls _persist_turns_async rather than the stale _persist_turns_ name.
- **UPDATED** `3360bc85-083` — Codex was right after checking the design note, main route gates, v3 pipeline/classifier, proxy v2 branch, live crontab, and CRON_JOBS.md. The old memory had an imprecise v3 path and omitted routing fallback and cron drift details.
- **UPDATED** `054561e1-e57` — Confirmed against the current spec, script, web search helper, CRON_JOBS.md, and live crontab; Codex was right that the old cron/model wording is stale while the legacy code flow still exists.
- **UPDATED** `ead0d5ad-0a7` — Codex was mostly right: code confirms package/signing/version/fallback behavior and lacks the Cloud Console project/registration state, but the local keystore still confirms the old full SHA-1.
- **UPDATED** `58f61412-713` — Current code confirms the Stage 2 text contract, registry path, consumed fields, and LOCAL_LLM default. Codex was mostly right, but live crontab is readable here and confirms auto_pull is active; corrected text also adds the abandon_pending control-dict exception.
- **UPDATED** `ef5612fc-3ee` — Verified the handler, metadata, dispatcher, v2 pipeline, v3 classifier, and v3 pipeline. Codex was right: the memory omitted the `weekly` loader and needed the v2/v3 params nuance; the rest matched current code.
- **KEPT** `d2d2e095-149` — Actual code confirms the memory: fetch_todo_list fetches/parses/writes the TODO cache with TODO_CACHE_PATH default, the handler reads it via _load_cache and supports read/add/remove flows, and configs plus live crontab both schedule fetch_todo_list.py every 30 minutes. Codex was wrong to leave live crontab unconfirmed here.
- **UPDATED** `5001554f-1cc` — Read the actual files and live crontab. The main memory is mostly correct, but it is truncated and incomplete about model override precedence; Codex was wrong for this environment about live `crontab -l` being unreadable, because the live entry is readable and present.
- **UPDATED** `eef09673-adb` — Code and runtime confirm the main Playwright/path/class claims, but the old memory was truncated and its historical 2026-05-24 launch verification was not evidenced in code; replaced it with a live 2026-06-08 verification. Codex was mostly right, though current `crontab -l` is not empty; cron is not part of this memory claim.
