# Memory Verification Report — 2026-06-07 02:02

Checked: 20 | Stale: 8 | Fixed: 8 | Deleted: 0 | Errors: 0 | Skipped recent: 186

- **UPDATED** `0b0896df-9a4` — Confirmed against configs/MCP_SPEC.md, jane/tool_loader.py, context_builder/v1/context_builder.py, jane_web/jane_proxy.py, agent_skills/essence_loader.py, and intent_classifier/v1-v3 code; Codex was directionally right but the memory needed the exact loader scope and manifest-based paths.
- **UPDATED** `6d032384-520` — Codex was right that the memory was stale. Source confirms USE_SERVER_TTS=false, active Android playback paths are local TTS, and the Daily Briefing generator is now a no-op; I also corrected that current BriefingViewModel has legacy server-audio helpers/cache code but does not use them for playback.
- **UPDATED** `81ce9c38-874` — Verified source and live crontab. The music resolver reuse claim is correct; the memory was truncated/partially stale around active model/router and cron details.
- **UPDATED** `909d51ad-8ee` — Verified against `jane_web/main.py`, installer ZIP scripts, `models.py`, and `gemma_router.py`. Codex was mostly right on artifacts/scripts/model references, but wrong that versioned installer names are served as direct file hits; the route only has alias glob resolution for installers and direct fallback for APKs.
- **UPDATED** `c9e822b9-0c7` — Code confirms the main APK release flow and no cron schedule. Codex was wrong that the main.py replacement regex fails: because it is unanchored, it matches inside `_ANDROID_VERSION_CODE` and updates that fallback value.
- **UPDATED** `44aab1b0-7fe` — Confirmed against models.py, news_fetcher.py, run_briefing.py, prune_articles.py, setup.sh, cron docs, and current crontab. Codex was mostly right, but the stale setup path resolves under `/home/chieh/ambient/tools`, not `/home/chieh/ambient/vessence/tools`, and the skill cron/jobs.json scheduler nuance should be recorded.
- **UPDATED** `b983d7f2-b4b` — Codex was right: the stale part is treating `JANE_USE_V2_PIPELINE=1` as a primary `main.py` chat-routing control; current code uses `JANE_USE_V3_PIPELINE` and `JANE_PIPELINE` there, while `JANE_USE_V2_PIPELINE` remains only in the legacy proxy branch.
- **UPDATED** `edc5dd77-b6a` — Confirmed against the actual script, backup crontab, and live crontab. Codex was right about the implementation, backup, and job order, but wrong here that live crontab could not be read; the original memory was truncated/partial.
