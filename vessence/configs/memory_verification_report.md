# Memory Verification Report — 2026-06-05 01:57

Checked: 20 | Stale: 10 | Fixed: 9 | Deleted: 0 | Errors: 0 | Skipped recent: 187

- **KEPT** `15937b95-ab3` — Code confirms non-pending regrade reports raise 'This regrade request has already been resolved.' before mutation, and teacher/TA routes return HTTP 409. The live app DB also confirms report_id=6, attempt_id=141, question_index=0, status=approved, reviewer_account_id=1, reviewed_at=2026-06-04T16:31:43, attempt_score=100.00, correct=true, credit=1.0. Codex was right about the code but wrong to treat the specific tuple as unverified in the available runtime state.
- **UPDATED** `236bb058-a93` — Codex was right to mark this partial: code confirms the proxy size-limit fix and auth behavior, but not the historical service restart or exact 25MB manual upload result.
- **UPDATED** `33f29c3b-dc8` — Actual code and live files confirm Codex's PARTIAL verdict, but Codex's suggested correction was slightly wrong about `essence_loader.py` using `SKILLS_DIR`, and live crontab was verifiable in this run.
- **UPDATED** `78c35991-0ca` — Code confirms the Gradle changelog gate and release flow. Codex was wrong that update_main_py no longer updates _ANDROID_VERSION_CODE at all: the unanchored regex still updates the fallback literal, though main.py now reads version.json as the runtime source of truth.
- **UPDATED** `d84f0c42-d66` — Confirmed from manifest, directory listing, rg, and sqlite inspection. Codex was wrong that category metadata is absent; topic/subtopic keys exist. Original memory was incomplete/imprecise about form template path and omitted crypto.
- **UPDATED** `3e6256d1-d80` — Confirmed against current code, job artifacts, live crontab, and .env. Codex was right that the queue-status part is valid and the old Gemma3/model-routing implementation details are stale.
- **UPDATED** `2f463691-64e` — Codex was right after checking `jane_web/jane_proxy.py`, `llm_brain/v1/persistent_claude.py`, and `jane_web/main.py`; the stale part was the unconditional 'only latest user message' claim and the reaper details needed clarification.
- **UPDATED** `1f69f5e7-dac` — Code confirms the shim and single BrainProcess claims, but the old openai/codex default was stale: startup/proxy now prefer standing_codex when JANE_WEB_STANDING_CODEX is not 0, and persistent_codex is only the fallback.
- **UPDATED** `deffa535-8a1` — Actual code confirms the shim, command flags, and OpenAI build branch. The stale part is the implied provider support: `_provider_uses_standing_process()` only returns true for Claude/Gemini, and OpenAI web routing uses Codex-specific paths.
- **UPDATED** `af8bee2b-9ef` — Verified against current repo files, latest installer artifacts, zip member lists, and `startup_code/installer_simulation.py`. Codex was right that the package paths are valid only inside the zips; the old memory was truncated and needed that clarification.
